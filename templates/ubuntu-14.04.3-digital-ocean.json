{
  "variables": {
    "api_token": "",
    "snapshot_name": "packer-hello-node"
  },
  "builders": [{
    "type": "digitalocean",
    "api_token": "{{user `api_token`}}",
    "snapshot_name": "{{user `snapshot_name`}}",
    "image": "ubuntu-14-04-x64",
    "region": "nyc1",
    "size": "512mb",
    "droplet_name": "hello-packer"
  }],
	"provisioners": [{
		"type": "shell",
    "script": "../scripts/app.sh"
	}]
}
